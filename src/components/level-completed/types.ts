import { JSX } from "react";

export interface ILevelDetails {
    color: string;
    gradient: string;
    grid: string;
    difficulty: string;
}

export interface IModeDetails {
    icon: JSX.Element;
    color: string;
    gradient: string;
    description: string;
}
